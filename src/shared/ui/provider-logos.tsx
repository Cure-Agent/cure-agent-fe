/**
 * 소셜 로그인 제공자의 브랜드 마크.
 *
 * `LogoMark`와 달리 색을 `currentColor`로 상속하지 않는다 — 세 마크 모두 각 사의
 * 등록 상표라 리컬러·비율 변경이 브랜드 가이드라인 위반이다. 색은 각 컴포넌트에
 * 하드코딩하고, 크기도 가이드가 정한 최소치가 있어 컴포넌트가 직접 갖는다.
 * 호출부는 위치만 정한다(`className`으로 `absolute` 등).
 *
 * 버튼이 심볼만 쓰는 아이콘형이라 화면에 라벨이 없다 — 접근 가능한 이름은 호출부의
 * `aria-label`이 지므로 마크는 전부 `aria-hidden`이다.
 */

type LogoProps = { className?: string };

/**
 * 구글 4색 G — 공식 배포본(48×48) 그대로. 단색화·테두리 추가 금지.
 * 가이드 권장 로고 크기는 40px 버튼 기준 18px.
 */
export function GoogleLogo({ className }: LogoProps): React.ReactElement {
  return (
    <svg
      viewBox="0 0 48 48"
      className={`h-[18px] w-[18px] ${className ?? ''}`}
      aria-hidden
      focusable="false"
    >
      <path
        fill="#EA4335"
        d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"
      />
      <path
        fill="#4285F4"
        d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"
      />
      <path
        fill="#FBBC05"
        d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"
      />
      <path
        fill="#34A853"
        d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"
      />
    </svg>
  );
}

/**
 * 카카오 말풍선 심볼.
 *
 * 카카오는 PNG·PSD만 배포하고 공식 SVG가 없다. 이 path는 공식 버튼 이미지
 * (kakao_login_large_wide.png)의 심볼과 픽셀 대조해 IoU 0.978로 일치를 확인한 것이며,
 * 차이는 전부 안티에일리어싱 경계다. 공식 SVG가 생기면 그쪽으로 교체할 것.
 *
 * 색 규정: 컨테이너 #FEE500, 심볼 #000000, 레이블 #000000 85%.
 *
 * 박스는 18px지만 viewBox가 23×21이라 실제 렌더 높이는 18 × 21/23 = **16.4px** 이다.
 * 공식 버튼의 심볼:버튼 높이 비 0.378 을 맞추려면 버튼 지름이 16.4 / 0.378 ≈ 43.4px 여야 한다
 * — 그래서 호출부가 44px(`h-11`)를 쓴다. **버튼 지름과 이 비율은 한 쌍이다**: 지름을 바꾸면
 * 여기 비율이 깨지므로 같이 확인할 것 (48px 였을 때 0.342 로 9.5% 벗어나 있었다).
 */
export function KakaoLogo({ className }: LogoProps): React.ReactElement {
  return (
    <svg
      viewBox="0 0 23 21"
      className={`h-[18px] w-[18px] ${className ?? ''}`}
      aria-hidden
      focusable="false"
    >
      <path
        fill="#000000"
        fillRule="evenodd"
        clipRule="evenodd"
        d="M11.0447 0C4.94804 0 0 3.84358 0 8.57074C0 11.5197 1.91074 14.0931 4.82655 15.6615L3.60058 20.1567C3.57745 20.2461 3.58221 20.3405 3.61423 20.4271C3.64624 20.5137 3.70398 20.5885 3.77969 20.6414C3.85541 20.6943 3.94549 20.7227 4.03784 20.723C4.1302 20.7232 4.22042 20.6952 4.2964 20.6427L9.66414 17.0752C10.117 17.0752 10.5809 17.1525 11.0447 17.1525C17.1414 17.1525 22.0895 13.309 22.0895 8.57074C22.0895 3.83254 17.1414 0 11.0447 0Z"
      />
    </svg>
  );
}

/**
 * 네이버 N 로고 타입.
 *
 * 공식 벡터 원본(NAVER_login_KR.ai)의 16px 변형에서 추출한 좌표다 — 공식 PNG 버튼과
 * 픽셀 대조 IoU 0.997. 로고 형태 변경·타 형태와의 조합은 금지되어 있다.
 *
 * 크기는 반드시 16px 이상이어야 한다(가이드: 완성형 16px, 아이콘형 18px 이상).
 * viewBox가 글리프에 꽉 맞으므로 박스 크기 = 실제 N 크기다. 줄이지 말 것.
 * 현재 버튼이 심볼만 쓰는 **아이콘형**이라 하한은 18px다 — 16px로 되돌리지 말 것.
 */
export function NaverLogo({ className }: LogoProps): React.ReactElement {
  return (
    <svg
      viewBox="0 0 16 16"
      className={`h-[18px] w-[18px] ${className ?? ''}`}
      aria-hidden
      focusable="false"
    >
      <path fill="#FFFFFF" d="M10.848 8.565L4.916 0H0V16H5.151V7.438L11.083 16H16V0H10.848Z" />
    </svg>
  );
}
