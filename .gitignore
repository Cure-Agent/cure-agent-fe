# dependencies
node_modules/

# next.js (7단계에서 도입)
.next/
out/

# env
.env
.env.*
!.env.example

# test
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# ide/os
.DS_Store
.idea/
*.log
*.tsbuildinfo

# 로컬 IDE MCP 연결 (개인 환경 전용 — 팀 공유 MCP 서버가 생기면 해제하고 커밋)
.mcp.json

# Codex — 프로젝트 스코프 스킬은 리포로 공유 (config.toml 등 로컬 머신 설정은 계속 무시)
.codex/*
!.codex/skills/

# SDD 하네스 스크래치 (BE implement.md Phase 2)
.cure-implement/

# Vercel 프로젝트/조직 ID (automation/pipeline.md 민감 파일 목록)
.vercel

# design-sync (claude.ai/design 동기화) — 스테이징 스크립트·빌드 산출물·머신 상태.
# .design-sync/ 의 나머지(config.json, NOTES.md, conventions.md, previews/, ds-preview/)는 커밋한다.
.ds-sync/
ds-bundle/
dist/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
